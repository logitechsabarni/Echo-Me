# Step 1: Initialize Git in your project root
git init

# Step 2: Add all files
git add .

# Step 3: Commit with a message
git commit -m "Initial commit for EchoMe full-stack project"

# Step 4: Add GitHub remote (replace with your repo link)
git remote add origin https://github.com/logitechsabarni/Echo-Me.git

# Step 5: Set the main branch
git branch -M main

# Step 6: Push your code to GitHub
git push -u origin main

